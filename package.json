{
  "name": "service-hive-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "client": "npm run dev -w packages/client",
    "server": "npm run dev -w packages/server",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build -w packages/shared && tsc -b"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}
